# Knowledge Base Semantic Search Benchmark

Система для семантичного пошуку по базі знань з підтримкою великих PDF документів.

## Можливості

- 📄 Обробка PDF, DOCX, TXT, MD файлів
- 🚀 Оптимізована робота з великими PDF (150+ сторінок)
- 📊 Прогрес-бар для відстеження обробки
- 🔍 Семантичний пошук через FAISS
- 🌐 Підтримка багатомовних моделей

## Встановлення

```bash
pip install -r requirements.txt
```

## Використання

### 1. Підготовка документів

Покладіть ваші документи (PDF, DOCX, TXT) в папку `data/raw/`:

```
data/raw/
  document1.pdf
  document2.pdf
  subfolder/
    document3.docx
```

### 2. Обробка та чанкінг

```bash
python src/ingest_chunk.py --input data/raw --output data/chunks.jsonl
```

**Опції:**
- `--min-words` - мінімальна кількість слів у чанку (за замовчуванням: 300)
- `--max-words` - максимальна кількість слів у чанку (за замовчуванням: 800)
- `--overlap` - перекриття між чанками в словах (за замовчуванням: 80)

**Для великих PDF:**
- Автоматичний прогрес-бар для файлів >50 сторінок
- Обробка помилок на рівні окремих сторінок
- Логування процесу обробки

### 3. Побудова індексу

```bash
python src/build_index.py --chunks data/chunks.jsonl --artifacts artifacts
```

**Опції:**
- `--model` - назва моделі SentenceTransformer (за замовчуванням: paraphrase-multilingual-MiniLM-L12-v2)
- `--batch-size` - розмір батчу для embeddings (за замовчуванням: 64)

### 4. Пошук

```bash
python src/query.py "ваш запит тут" --artifacts artifacts
```

**Опції:**
- `--top-k` - скільки найбільш релевантних чанків повернути (за замовчуванням: 5)
- `--full-text` - показати повний текст чанку, а не тільки перші 300 символів
- `--json` - вивести результати в JSON форматі
- `--max-snippet` - розмір уривку якщо не використовується `--full-text`

**Приклади:**

```bash
# Базовий пошук - поверне 5 найрелевантніших чанків
python src/query.py "що таке машинне навчання"

# Показати повний текст знайдених чанків
python src/query.py "що таке машинне навчання" --full-text

# Знайти більше результатів
python src/query.py "що таке машинне навчання" --top-k 10

# Вивести в JSON для подальшої обробки
python src/query.py "що таке машинне навчання" --json
```

## Рекомендації для великих PDF

### ❓ Чанки vs Батчі - в чому різниця?

**Важливо розуміти:**

- **Чанк (chunk)** - це одна частина документа (наприклад, 300-800 слів). Це те, що ви отримаєте в результатах пошуку.
- **Батч (batch)** - технічний параметр для обробки векторів (не впливає на пошук!).

**Як працює пошук:**

1. Ваш PDF на 150 сторінок → ділиться на ~200 чанків
2. Кожен чанк отримує векторне представлення
3. При пошуку повертаються **тільки найбільш релевантні чанки** (за замовчуванням 5)
4. Ви **НЕ** отримаєте весь документ, а тільки ті частини, які релевантні запиту

**Приклад:**
```bash
# У вас 5 PDF файлів → 1000 чанків всього
python src/query.py "штучний інтелект" --top-k 5
# Поверне лише 5 найбільш релевантних чанків з усіх 1000!
```

### Оптимальні налаштування

Для PDF файлів по 150 сторінок:

```bash
# Менші чанки для кращої точності
python src/ingest_chunk.py --min-words 200 --max-words 500 --overlap 50

# Більший batch-size якщо є достатньо GPU пам'яті
python src/build_index.py --batch-size 128
```

### Очікувана продуктивність

- **1 PDF (150 сторінок)**: ~1-2 хвилини обробки
- **10 PDF файлів**: ~10-15 хвилин
- **Пам'ять**: ~2-4 GB для процесу індексування

### Моніторинг процесу

Під час обробки ви побачите:
- Прогрес читання кожного великого PDF
- Кількість створених чанків з кожного файлу
- Попередження про помилкові сторінки
- Загальну статистику після завершення

## Структура проекту

```
.
├── data/
│   ├── raw/              # Оригінальні документи
│   └── chunks.jsonl      # Обробленні чанки
├── artifacts/
│   ├── faiss.index       # FAISS індекс
│   ├── vectors.npy       # Векторні представлення
│   └── meta.jsonl        # Метадані чанків
└── src/
    ├── ingest_chunk.py   # Обробка та чанкінг
    ├── build_index.py    # Побудова індексу
    └── query.py          # Пошук
```

## Troubleshooting

### Помилки читання PDF
Якщо окремі сторінки не читаються - вони будуть пропущені з попередженням у логах.

### Мало пам'яті
Зменшіть `--batch-size` до 32 або 16.

### Повільна обробка
Перевірте логи - можливо багато помилок при читанні PDF. Спробуйте конвертувати PDF в іншому редакторі.
